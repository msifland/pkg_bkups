-These are Nautilus actions that are added to right-click menu.

-Install filemanager-actions:
	sudo apt install filemanager-actions

-Open filemanager-actions in apps menu.

-Copy the .desktop files(which were exported from fm-a) to the local directory: $HOME/.local/share/file-manager/actions

-Now they should appear in the Nautilus menus.
