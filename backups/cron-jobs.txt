# Example, you can run a backup of all your user accounts
# at 5 a.m every week with:
# 0 5 * * 1 tar -zcf /var/backups/home.tgz /home/
# 
# For more information see the manual pages of crontab(5) and cron(8)
# 
# m h  dom mon dow   command

## The jobs with the env DISPLAY=:0 opens the jobs in a terminal

## The read -s at the end of some commands caused the terminal to stay open and wait for any key to be pressed to close. This is zsh specific, if using bash to source the use read -n1
########################### Ubuntu Linux #########################
# Update system daily. Different here because we a calling more than one command. 
49 10 * * * /home/msifland/scripts/cron_scripts/linux-update-run > $HOME/tmp/output.txt

# Backups my important files. See script for files that are backed up
32 9 * * * env DISPLAY=:0 gnome-terminal -- /home/msifland/scripts/my-backup

# Scans for viruses once a week
30 1 * * 1 env DISPLAY=:0 gnome-terminal -- zsh -c "source .zshrc; /home/msifland/scripts/virus-clamscan; read -s"

# Restart PC once a week
0 3 * * 6 env DISPLAY=:0 gnome-terminal -- /home/msifland/scripts/restart

# Run a check for new kernels
45 6 * * * env DISPLAY=:0 gnome-terminal -- zsh -c "source .zshrc; /home/msifland/scripts/kernel_scripts/kernel-check"

# Update oh-my-zsh once a week
30 7 * * 2 env DISPLAY=:0 gnome-terminal -- zsh -c "source .zshrc; oh-my-zsh-updater; read -s"

# Build a new OSM map for my Garmin on the first day of every month at 8pm
#00 19 1 * * env DISPLAY=:0 gnome-terminal -- zsh -c "source .zshrc; /home/msifland/scripts/map-my-builder; read -s"

